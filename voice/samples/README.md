Your published writing goes here, as markdown.

Filename: `YYYY-MM-DD - Title.md`. Frontmatter should carry `format:`
(article | chapter | social) and `platform:` so drafting can match the register.

Three to five samples per format is enough. Until this folder has content,
`/draft` will tell you the voice is unverified rather than guessing at it.
