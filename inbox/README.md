Drop zone. Anything that landed here has not been given a citation key yet.

Web Clipper output, pasted transcripts, screenshots of things to read later —
put it here and it is safe. `/capture` turns anything in this folder into a
proper source file with a key.

Files here are not citable and are not searched by drafting. That is the only
difference from `sources/`.
